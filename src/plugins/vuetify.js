import { createVuetify } from 'vuetify'; // Certifique-se de importar corretamente
import 'vuetify/styles'; // Importa os estilos do Vuetify

const vuetify = createVuetify(); // Cria uma instância do Vuetify

export default vuetify;
